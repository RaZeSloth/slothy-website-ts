import React from 'react';
const { Database } = require("quickmongo");
const db = new Database(" ")
const profile = () => { 
   return (
      <div>
         
         <h1>Profile</h1>
         <p>Still wip</p>
      </div>
   );
}

export default profile;

